# Regression-Tree-Guided Probabilistic Hull Generation

本项目实现了基于回归树引导的概率外壳生成方法，用于神经网络概率验证。

## 📋 目录

- [项目简介](#项目简介)
- [环境配置](#环境配置)
- [快速开始](#快速开始)
- [项目结构](#项目结构)
- [核心算法](#核心算法)
- [实验结果](#实验结果)
- [常见问题](#常见问题)
- [引用](#引用)

## 🎯 项目简介

本项目提出了一种回归树引导的概率外壳生成方法，用于神经网络的概率验证。主要特点包括：

- **智能采样策略**：边界感知采样和分布引导采样
- **回归树引导分区**：自适应区域划分，基于概率质量引导的分区策略
- **概率验证**：基于 CROWN 边界计算的安全概率估计

## 🔧 环境配置

### 系统要求

- Python 3.8+ (auto_LiRPA 要求 Python 3.7+)
- PyTorch >=1.11.0, <2.3.0 (auto_LiRPA 的严格版本要求)
- CUDA 11.1+ (可选，用于 GPU 加速)
- 至少 8GB RAM (大型网络建议 16GB+)

### 安装方法

#### 方法 1: 使用 Conda (推荐)

```bash
# 创建 conda 环境
conda env create -f environment.yml

# 激活环境
conda activate prob-verification

# 安装 auto_LiRPA (作为子模块)
cd auto_LiRPA
pip install -e .

# 可选：构建 CUDA 模块以加速计算
python auto_LiRPA/cuda_utils.py install
```

#### 方法 2: 使用 pip

```bash
# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或 venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt

# 安装 auto_LiRPA (作为子模块)
cd auto_LiRPA
pip install -e .

# 可选：构建 CUDA 模块
python auto_LiRPA/cuda_utils.py install
```

**重要提示**：auto_LiRPA 需要作为子模块包含在项目中。如果使用 Git 克隆，请运行：
```bash
git submodule update --init --recursive
```

## 🚀 快速开始

### 运行实验

1. **ACAS Xu 实验 (ReLU 网络)**:
   ```bash
   cd src
   python acas.py
   ```

2. **ACAS Xu 实验 (Tanh 网络)**:
   ```bash
   cd src
   python acas_tanh.py
   ```

3. **RocketNet 实验**:
   ```bash
   cd src
   python rocketnet.py
   ```

4. **基线对比实验 (朴素分支定界)**:
   ```bash
   cd src
   python naive_bab.py       # ACAS ReLU 网络基线
   python bab_tanh.py        # ACAS Tanh 网络基线
   ```

5. **2D 示例演示**:
   ```bash
   cd src
   python toy_example.py
   ```

### 实验结果

所有实验结果将保存在 `results/` 目录中：
- `results/acas_experiments/` - ACAS 实验结果
- `results/tanh_experiments/` - Tanh 网络实验结果
- `results/rocketnet_experiments/` - RocketNet 实验结果
- `results/naive_bab/` 和 `results/bab_tanh/` - 基线方法结果
- `results/comparison/` - 可视化结果

## 📁 项目结构

```
├── src/                          # 源代码目录
│   ├── acas.py                  # ACAS Xu ReLU 网络实验主程序
│   ├── acas_tanh.py             # ACAS Xu Tanh 网络实验主程序
│   ├── rocketnet.py             # RocketNet 实验主程序
│   ├── naive_bab.py             # 基线方法 (ACAS ReLU)
│   ├── bab_tanh.py              # 基线方法 (ACAS Tanh)
│   ├── toy_example.py           # 2D 示例演示
│   ├── construct_acas_tanh.py   # ACAS Tanh 网络构建工具
│   │
│   ├── utils/                   # 工具函数
│   │   ├── load.py              # 模型加载函数
│   │   ├── utils.py             # 核心计算工具
│   │   └── __init__.py
│   │
│   ├── samplers/                # 采样器模块
│   │   ├── uniform_boundary_sampler.py      # 均匀边界采样
│   │   ├── distribution_boundary_sampler.py # 分布边界采样
│   │   └── __init__.py
│   │
│   ├── regression_tree/         # 回归树模块
│   │   ├── tree_builder.py      # 决策树构建器
│   │   └── __init__.py
│   │
│   ├── models/                  # 网络模型
│   │   ├── tiny_network.py     # 2D 示例网络
│   │   ├── deep_network_2d.py  # 深度 2D 网络
│   │   └── __init__.py
│   │
│   └── visualization/           # 可视化工具
│       ├── vis_utils.py         # 可视化函数
│       └── __init__.py
│
├── checkpoints/                 # 预训练模型 (见 checkpoints/README.md)
│   ├── acas/                   # ACAS ReLU 模型
│   ├── acas_tanh/              # ACAS Tanh 模型
│   └── RocketNetReLU/          # RocketNet 模型
│
├── docs/                        # 文档
│   └── artifacts.md            # 资源获取说明
│
├── requirements.txt             # Python 依赖
├── environment.yml              # Conda 环境配置
└── README.md                    # 本文件
```

## 🔬 核心算法

本项目实现了以下关键算法：

1. **智能采样策略**：
   - 边界感知采样
   - 分布引导采样
   - 混合采样策略

2. **回归树引导分区**：
   - 自适应区域划分
   - 概率质量引导的分区策略
   - 并行化树构建

3. **概率验证**：
   - CROWN 边界计算
   - 安全概率估计
   - 收敛检查

## 📊 实验结果

每个实验输出以下关键指标：

- **Ls**: 下界安全概率
- **Us**: 上界安全概率
- **Us-Ls**: 概率区间宽度
- **time**: 运行时间 (秒)

较小的 `Us-Ls` 值表示更精确的概率估计。

### 可重现性说明

所有主程序都设置了固定的随机种子以确保可重现的结果：

```python
torch.manual_seed(1024)
np.random.seed(1024)
```

关键实验参数（在脚本中固定）：
- **总采样数**: 1000 (ACAS), 9000 (RocketNet)
- **增量采样数**: 100 (ACAS), 900 (RocketNet)
- **最大树深度**: 10
- **未知概率阈值**: 1e-5 (ACAS), 1e-3 (RocketNet)
- **系数参数**: 0.3

## ⚠️ 常见问题

### 常见错误

1. **auto_LiRPA 导入错误**
   - 确保在 auto_LiRPA 目录中正确安装了 auto_LiRPA：`pip install -e .`
   - 检查 PyTorch 版本是否在支持范围内 (>=1.11.0, <2.3.0)

2. **PyTorch 版本冲突**
   - auto_LiRPA 要求 PyTorch >=1.11.0, <2.3.0
   - 如有必要，请更新或降级 PyTorch

3. **CUDA 错误**
   - 检查 GPU 内存和 CUDA 版本与 PyTorch 和 auto_LiRPA 的兼容性

4. **导入错误**
   - 确保所有依赖项都按照 requirements.txt 中的确切版本要求正确安装

5. **路径错误**
   - 从项目根目录运行脚本
   - 确保 auto_LiRPA 子模块可用

6. **内存问题**
   - 考虑减少采样数量或使用较小的网络

### 获取帮助

如果遇到问题，请检查：
1. auto_LiRPA 是否正确安装，所有依赖版本是否匹配 requirements.txt
2. PyTorch 版本是否在支持范围内 (1.11.0 <= version < 2.3.0)
3. 是否从正确的目录运行，auto_LiRPA 子模块是否可用
4. 是否有足够的 GPU 内存
5. Python 版本兼容性 (推荐 3.8+)

## 📚 引用

如果您在研究中使用了本代码，请引用相应的论文：

```bibtex
@inproceedings{your-paper,
  title={Regression-Tree-Guided Probabilistic Hull Generation},
  author={Your Name},
  booktitle={Conference Name},
  year={2025}
}
```

## 📄 许可证

本项目采用 [MIT License](LICENSE) 许可证。详情请参阅 LICENSE 文件。

## 🔗 相关资源

- **模型权重**: 请参阅 [checkpoints/README.md](checkpoints/README.md) 了解如何获取预训练模型
- **实验资源**: 请参阅 [docs/artifacts.md](docs/artifacts.md) 了解额外的实验数据和资源

